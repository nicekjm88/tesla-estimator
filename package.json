{
  "name": "tesla-estimator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "npm run build && npm run export && gh-pages -d out"
  },
  "dependencies": {
    "antd": "^5.13.0",
    "next": "15.1.8",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
